<Window x:Class="AppleOrderManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppleOrderManagement"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="红富士苹果订单管理程序2016" MinHeight="400" MinWidth="600" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="Wheat">
            <Button Content="订单" Style="{StaticResource ButtonBasicNavigation}" Click="Order_Click"/>
            <Button Content="提权" Style="{StaticResource ButtonBasicNavigation}" Click="LogIn_Click"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <ContentControl x:Name="MainContent"/>
        </Grid>
    </Grid>
</Window>
